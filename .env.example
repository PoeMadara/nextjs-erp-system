# Environment variables declared in this file are examples.
# Create a .env.local file in the root of your project and populate it with your actual values.
# DO NOT commit your .env.local file to version control.

# Database Configuration (Choose one section and adapt to your DB)

# Example for PostgreSQL:
# DB_TYPE=postgres
# DB_HOST=your_postgres_host
# DB_PORT=5432
# DB_USER=your_postgres_user
# DB_PASSWORD=your_postgres_password
# DB_NAME=your_postgres_database_name
# DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=require"

# Example for MySQL:
# DB_TYPE=mysql
# DB_HOST=your_mysql_host
# DB_PORT=3306
# DB_USER=your_mysql_user
# DB_PASSWORD=your_mysql_password
# DB_NAME=your_mysql_database_name
# DATABASE_URL="mysql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}"

# Example for MongoDB Atlas (Connection String):
# DB_TYPE=mongodb
# MONGODB_URI="your_mongodb_atlas_connection_string"

# Firebase Configuration (if using Firebase services like Firestore, Auth directly)
# These are typically handled by Firebase SDK initialization files, but you might have custom backend keys.
# NEXT_PUBLIC_FIREBASE_API_KEY=
# NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
# NEXT_PUBLIC_FIREBASE_PROJECT_ID=
# NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
# NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
# NEXT_PUBLIC_FIREBASE_APP_ID=
# NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=

# Genkit/Google AI Configuration (if applicable for GenAI features)
# GOOGLE_API_KEY=your_google_ai_api_key

# NextAuth.js Configuration (if you switch to NextAuth.js for authentication)
# NEXTAUTH_URL=http://localhost:3000 # Your app's base URL
# NEXTAUTH_SECRET= # Generate a strong secret: `openssl rand -base64 32`

# Application Specific
# NEXT_PUBLIC_APP_NAME="ERP System"

