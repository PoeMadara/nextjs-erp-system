
import NotificationClientPage from './NotificationClientPage';

export const metadata = {
  title: 'Gestión de Notificaciones - ERP System', // Title in Spanish for default
  // English title can be set dynamically if needed, or use a generic one
};

export default function NotificacionesPage() {
  return <NotificationClientPage />;
}
